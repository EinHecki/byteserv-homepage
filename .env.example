# ===================================
# SMTP Configuration für E-Mail-Versand
# ===================================
#
# Das Kontaktformular verwendet diese Einstellungen, um Anfragen per E-Mail zu versenden.
# Kopiere diese Datei nach .env und fülle die Werte mit deinen tatsächlichen Zugangsdaten.

# SMTP Server Konfiguration
SMTP_HOST=mail.deinserver.de
SMTP_PORT=465
SMTP_USER=kontakt@byteserv.it
SMTP_PASS=dein_passwort_hier

# E-Mail Adressen
# SMTP_FROM: Absender-Adresse (muss oft mit SMTP_USER übereinstimmen)
# SMTP_TO: Empfänger-Adresse, an die Kontaktformular-Anfragen gesendet werden
SMTP_FROM=kontakt@byteserv.it
SMTP_TO=hello@byteserv.it

# ===================================
# Beispiele für gängige Provider
# ===================================

# --- Gmail ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=deine@gmail.com
# SMTP_PASS=dein_app_passwort  # Wichtig: App-Passwort verwenden, nicht normales Passwort!
# Mehr Infos: https://support.google.com/accounts/answer/185833

# --- Microsoft 365 / Outlook ---
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=deine@outlook.com
# SMTP_PASS=dein_passwort

# --- Eigener Server / Plesk / cPanel ---
# SMTP_HOST=mail.deine-domain.de
# SMTP_PORT=465  # oder 587 für TLS
# SMTP_USER=deine@deine-domain.de
# SMTP_PASS=dein_passwort

# ===================================
# Hinweise
# ===================================
# - Port 465: SSL (secure)
# - Port 587: TLS/STARTTLS (empfohlen)
# - Ohne diese Konfiguration wird keine E-Mail versendet, aber das Formular funktioniert weiterhin
#   (Anfragen werden dann nur in der Console geloggt)
